const multiply = (x, y) => {
	return x * y;
}

const sub = (x, y) => {
	return x - y;
}

const add = (x, y) => {
	return x + y;
}

const add2 = (x, y) => {
	return x + y;
}

module.exports = { multiply, sub, add, add2 };