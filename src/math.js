const multiply = (x, y) => {
	return x * y;
}

const multiply2 = (x, y) => {
	return x * y;
}

const sub = (x, y) => {
	return x - y;
}

const sub2 = (x, y) => {
	return x - y;
}

const add = (x, y) => {
	return x + y;
}

const add2 = (x, y) => {
	return x + y;
}

module.exports = { multiply, multiply2, sub, sub2, add, add2 };